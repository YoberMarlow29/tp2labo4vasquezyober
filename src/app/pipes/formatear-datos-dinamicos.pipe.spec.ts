import { FormatearDatosDinamicosPipe } from './formatear-datos-dinamicos.pipe';

describe('FormatearDatosDinamicosPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearDatosDinamicosPipe();
    expect(pipe).toBeTruthy();
  });
});
